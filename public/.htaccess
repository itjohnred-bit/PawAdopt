<FilesMatch "^(database|email)\.php$">
    Require all denied
</FilesMatch>

<FilesMatch "^\.ht">
    Require all denied
</FilesMatch>
<FilesMatch "\.(env|sql|bak|backup|gitignore|git|svn|log)$">
    Require all denied
</FilesMatch>

<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "SAMEORIGIN"
    Header set X-XSS-Protection "1; mode=block"
    Header set Referrer-Policy "strict-origin-when-cross-origin"
    Header unset X-Powered-By
    Header unset Server
</IfModule>

<IfModule mod_rewrite.c>
    RewriteEngine On

    # 1. API Passthrough: Stop processing if the request is for the /api folder
    RewriteRule ^api/ - [L]

    # 2. Favicon rewrite: ONLY trigger if the requested URI is exactly /favicon.ico
    RewriteCond %{REQUEST_URI} ^/favicon\.ico$ [NC]
    RewriteRule .* /assets/images/Paw-Adopt%20Logo.png [L]

    # 3. Pretty URLs: Convert /name to /name.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME}.php -f
    RewriteRule ^([^\.]+)$ $1.php [L]
</IfModule>

<IfModule mod_mime.c>
    AddDefaultCharset UTF-8
    AddCharset UTF-8 .html .css .js .json .xml .txt
</IfModule>

<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/png  "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType text/css   "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
</IfModule>